[
  {
    "vars": {
      "pr_context": "PR: Unified Storage Performance Optimizations\nThis PR optimizes the search index building by reducing lock scope in BuildIndex to allow concurrent builds.",
      "finding": "[0] pkg/storage/unified/search/bleve.go:104-141\n  Description: Race condition introduced by reducing lock scope in BuildIndex. The previous code held cacheMu for the entire function, serializing all index builds. The new code only locks during cache assignment, allowing multiple goroutines to concurrently build indexes for the same key, causing file corruption and resource leaks.\n  Existing code: b.cacheMu.Lock() // moved to only cache write\n  Suggested fix: Restore lock at function start or implement per-key locking"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Race condition in BuildIndex"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Frontend Asset Optimization\nThis PR adds RWMutex-based caching for web assets to avoid redundant file reads.",
      "finding": "[0] pkg/api/webassets/webassets.go:48-52\n  Description: Missing second check in double-checked locking pattern. After acquiring the write lock, the code must re-verify cache is nil. Without this, multiple goroutines will sequentially acquire the lock and redundantly compute the same result.\n  Existing code: entryPointAssetsCacheMu.Lock() defer Unlock()\n  Suggested fix: Add if entryPointAssetsCache != nil { return } after lock"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Double-checked locking missing second check"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Advanced SQL Analytics Framework\nAdds SQL expression support with feature flag control.",
      "finding": "[0] pkg/expr/reader.go:195-200\n  Description: Logic error in enableSqlExpressions: The function always returns false regardless of the feature flag state. The negation operator inverts the check, and both branches return false.\n  Existing code: enabled := !h.features.IsEnabledGlobally(featuremgmt.FlagSqlExpressions)\n  Suggested fix: return h.features.IsEnabledGlobally(featuremgmt.FlagSqlExpressions)"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Feature flag always returns false"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Advanced Query Processing Architecture\nRefactors query splitting to apply template variables before shard splitting.",
      "finding": "[0] public/app/plugins/datasource/loki/shardQuerySplitting.ts:52-55\n  Description: Template variables and ad-hoc filters are applied twice when shard splitting is used. runShardSplitQuery applies applyTemplateVariables, then runSplitQuery applies them again, causing duplicate filter injection.\n  Existing code: const interpolated = queries.map(q => datasource.applyTemplateVariables(q))\n  Suggested fix: Remove from runShardSplitQuery since runSplitQuery handles it"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Template vars applied twice"
    }
  },
  {
    "vars": {
      "pr_context": "PR: AuthZService: improve authz caching\nAdds denial caching to reduce database load for authorization checks.",
      "finding": "[0] pkg/services/authz/rbac/service.go:116-121\n  Description: Cache consistency issue: permDenialCache is checked before permCache, and both have independent 30-second TTLs. When permissions are granted after a denial was cached, users remain denied for up to 30 seconds.\n  Existing code: if denied, ok := s.permDenialCache.Get(key); ok { return false }\n  Suggested fix: Check permCache first or invalidate denial entries on permission changes"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Denial cache not invalidated after grant"
    }
  },
  {
    "vars": {
      "pr_context": "PR: FEATURE: Can edit category/host relationships for embedding\nAdds CRUD for embeddable hosts with admin controller.",
      "finding": "[0] app/controllers/admin/embeddable_hosts_controller.rb:18\n  Description: Nil reference vulnerability in destroy action. EmbeddableHost.where(id: params[:id]).first returns nil if record doesn't exist, calling host.destroy on nil causes NoMethodError.\n  Existing code: host = EmbeddableHost.where(id: params[:id]).first; host.destroy\n  Suggested fix: return render_json_error(404) unless host"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "NoMethodError nil dereference"
    }
  },
  {
    "vars": {
      "pr_context": "PR: OAuth credential sync and app integration enhancements\nAdds webhook endpoint for credential synchronization between instances.",
      "finding": "[0] apps/web/pages/api/webhook/app-credential.ts:29\n  Description: Timing attack vulnerability in webhook secret comparison. Direct string comparison using !== short-circuits on first mismatch, allowing attackers to measure response times.\n  Existing code: if (req.headers[headerName] !== process.env.CALCOM_WEBHOOK_SECRET)\n  Suggested fix: Use crypto.timingSafeEqual"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Timing attack webhook secret"
    }
  },
  {
    "vars": {
      "pr_context": "PR: SMS workflow reminder retry count tracking\nAdds retry counting for failed SMS reminders with cleanup of expired ones.",
      "finding": "[0] packages/features/ee/workflows/api/scheduleSMSReminders.ts:43\n  Description: Logic error in deleteMany query: The OR condition's second branch lacks method filter. Deletes EMAIL and WHATSAPP reminders with high retryCount, not just SMS.\n  Existing code: OR: [{ scheduledDate: { lte: ... } }, { retryCount: { gt: 1 } }]\n  Suggested fix: Add method: WorkflowMethods.SMS to retryCount condition"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "OR condition deletes wrong reminder types"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Optimize spans buffer insertion with eviction during insert\nOptimizes the span buffer pagination with a new OptimizedCursorPaginator.",
      "finding": "[0] src/sentry/api/paginator.py:877-882\n  Description: OptimizedCursorPaginator allows negative offset slicing on Django QuerySets. Django doesn't support negative indexing — causes AssertionError.\n  Existing code: queryset[cursor.offset:stop]\n  Suggested fix: Validate offset is non-negative before slicing"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Negative offset causes Django crash"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Replays Self-Serve Bulk Delete System\nAdds bulk delete functionality for replays with breadcrumb summarization.",
      "finding": "[0] src/sentry/replays/endpoints/project_replay_summarize_breadcrumbs.py:140\n  Description: The get_request_data function mutates the input error_events list in-place with .sort(). Callers don't expect their list to be modified.\n  Existing code: error_events.sort(key=lambda x: x['timestamp'])\n  Suggested fix: Use sorted() to return new list"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Mutating input list in-place"
    }
  },
  {
    "vars": {
      "pr_context": "PR: FIX: proper handling of group memberships\nRefactors group member management with pagination and CRUD operations.",
      "finding": "[0] app/controllers/admin/groups_controller.rb:75\n  Description: N+1 query in add_members. Loop calls User.find_by_username individually. With 1000 usernames, executes 1000 queries.\n  Existing code: usernames.each { |u| User.find_by_username(u) }\n  Suggested fix: User.where(username: usernames)"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "N+1 query in loop"
    }
  },
  {
    "vars": {
      "pr_context": "PR: FIX: proper handling of group memberships\nRefactors group member management with pagination.",
      "finding": "[0] app/controllers/groups_controller.rb:22\n  Description: Unbounded limit parameter allows DoS attacks. Limit converted to integer without max validation.\n  Existing code: limit = params[:limit].to_i\n  Suggested fix: limit = [params[:limit].to_i, 100].min"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Unbounded limit parameter DoS"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Advanced SQL Analytics Framework\nAdds SQL expression support with parser and DB stubs.",
      "finding": "[0] pkg/expr/sql/parser.go:26\n  Description: SQL injection via string interpolation in stub code that always returns 'not implemented'. Only single quotes escaped.\n  Existing code: fmt.Sprintf(\"SELECT json_serialize_sql('%s')\", rawSQL)\n  Suggested fix: Use parameterized queries"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "SQL injection in stub code"
    }
  },
  {
    "vars": {
      "pr_context": "PR: AuthZService: improve authz caching\nAdds denial caching to reduce database load.",
      "finding": "[0] pkg/services/authz/rbac/service.go:116\n  Description: Unbounded cache growth in permDenialCache. Cache key includes high-cardinality name and parentFolder. Creates ~1M entries for 1000 users × 1000 resources.\n  Existing code: s.permDenialCache.Set(key, true)\n  Suggested fix: Add size limits or remove per-resource denial caching"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Unbounded cache growth"
    }
  },
  {
    "vars": {
      "pr_context": "PR: feat: 2fa backup codes\nAdds backup code generation and verification for two-factor auth.",
      "finding": "[0] apps/web/components/auth/BackupCode.tsx:7\n  Description: Component function named TwoFactor instead of BackupCode. Copy-paste error from TwoFactor.tsx.\n  Existing code: export default function TwoFactor() {\n  Suggested fix: export default function BackupCode() {"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Wrong component name (copy-paste)"
    }
  },
  {
    "vars": {
      "pr_context": "PR: feat: 2fa backup codes\nAdds backup code generation for two-factor authentication.",
      "finding": "[0] apps/web/pages/api/auth/two-factor/totp/setup.ts:61\n  Description: Backup codes use only 40 bits of entropy (5 bytes). Should use at least 80 bits.\n  Existing code: crypto.randomBytes(5).toString('hex')\n  Suggested fix: crypto.randomBytes(10).toString('hex')"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Backup code entropy too low"
    }
  },
  {
    "vars": {
      "pr_context": "PR: OAuth credential sync and app integration enhancements\nAdds credential sync between Cal.com instances via webhook.",
      "finding": "[0] packages/app-store/_utils/oauth/refreshOAuthTokens.ts:14\n  Description: CALCOM_CREDENTIAL_SYNC_ENDPOINT environment variable used in fetch without URL validation. Could allow SSRF.\n  Existing code: fetch(process.env.CALCOM_CREDENTIAL_SYNC_ENDPOINT)\n  Suggested fix: Validate URL protocol and domain"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "SSRF via environment variable"
    }
  },
  {
    "vars": {
      "pr_context": "PR: feat: 2fa backup codes\nAdds backup code verification for disabling 2FA.",
      "finding": "[0] apps/web/pages/api/auth/two-factor/totp/disable.ts:52\n  Description: Missing rate limiting on backup code verification endpoint allows brute force attacks.\n  Existing code: // no rate limit check\n  Suggested fix: Add checkRateLimitAndThrowError(req)"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Missing rate limiting on endpoint"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Frontend Asset Optimization",
      "finding": "[0] pkg/api/webassets/webassets.go:49\n  Description: Double-checked locking pattern is incomplete. After acquiring the write lock at line 48, the code must re-verify that entryPointAssetsCache is still nil before reloading assets. Without this second check, if two goroutines race, both will perform the expensive asset loading operation even though onl"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "The GetWebAssets function implements an incomplete double-ch..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: Dual Storage Architecture",
      "finding": "[0] pkg/apiserver/rest/dualwriter_mode3.go:47\n  Description: The Create method calls recordLegacyDuration when recording storage operation metrics on error. This should be recordStorageDuration since it's measuring the Storage.Create call."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Bug: calling recordLegacyDuration when storage operation fai..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: Dual Storage Architecture",
      "finding": "[0] pkg/apiserver/rest/dualwriter_mode3.go:106\n  Description: In the Delete method, the success case passes 'name' (object name) as the metric label, but all other calls use 'options.Kind' (the resource kind). This inconsistency will result in different label values for the same metric."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Inconsistency: using name instead of options.Kind for metric..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: Dual Storage Architecture",
      "finding": "[0] pkg/apiserver/rest/dualwriter_mode3.go:97\n  Description: Inconsistent context usage in Delete method: line 97 creates a logger context with `d.Log` instead of `log` (which includes name/kind/method values), unlike all other methods. This loses the structured logging fields for the async goroutine."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "The context is being created with d.Log instead of the log v..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: feat(upsampling) - Support upsampled error count with performance optimizations",
      "finding": "[0] src/sentry/api/helpers/error_upsampling.py:27\n  Description: Python's `hash()` function is non-deterministic across process restarts due to hash randomization (PYTHONHASHSEED). In multi-process deployments (typical for production), each worker process generates different hash values for identical inputs, causing cache misses 100% of the time and making the 60"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Using Python’s built-in hash() to build cache keys is non-de..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: ref(crons): Reorganize incident creation / issue occurrence logic",
      "finding": "[0] src/sentry/monitors/logic/incident_occurrence.py:171\n  Description: Dead computation in get_monitor_environment_context(). The function creates a copy of monitor.config, modifies the copy's schedule_type to use the display value, but then returns the original unmodified monitor.config. The 'config' variable is computed but never used."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "The function modifies the config variable to include display..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: Span Buffer Multiprocess Enhancement with Health Monitoring",
      "finding": "[0] src/sentry/spans/consumers/process/flusher.py:199\n  Description: Inconsistent metric tag keys causing metrics aggregation issues. The 'spans.buffer.flusher.produce' timer uses tag 'shard' while 'spans.buffer.flusher.wait_produce' uses 'shards'. These should be consistent."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Inconsistent metric tagging with 'shard' and 'shards'"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Add HTML sanitizer for translated message resources",
      "finding": "[0] misc/theme-verifier/src/main/java/org/keycloak/themeverifier/VerifyMessageProperties.java:150\n  Description: Method name 'santizeAnchors' contains a typo and should be 'sanitizeAnchors' for consistency and code clarity. The typo appears in both the method definition and the call site."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "The method name 'santizeAnchors' should be 'sanitizeAnchors'..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: Add AuthzClientCryptoProvider for authorization client cryptographic operations",
      "finding": "[0] authz/client/src/main/java/org/keycloak/authorization/client/util/crypto/AuthzClientCryptoProvider.java:116\n  Description: Lines 114-115 create two ASN1Encoder instances and write BigInteger values to them, but the results are never used. These are dead computations that waste resources. The actual encoding that returns the result happens on lines 117-121."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "Dead code exists where ASN1Encoder instances are created and..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: FEATURE: Localization fallbacks (server-side)",
      "finding": "[0] lib/freedom_patches/translate_accelerator.rb:65\n  Description: Race condition in `ensure_loaded!` method. The check-then-act pattern (`@loaded_locales.include?(locale)` followed by `load_locale`) is not atomic. Multiple threads can pass the check before either enters `load_locale`, causing redundant work. While `load_locale` has internal mutex protection, the o"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Thread-safety issue with lazy @loaded_locales"
    }
  },
  {
    "vars": {
      "pr_context": "PR: Optimize header layout performance with flexbox mixins",
      "finding": "[0] app/assets/stylesheets/common/foundation/mixins.scss:123\n  Description: Invalid CSS property `-ms-align-items` on line 121. This property never existed in any browser specification. Internet Explorer's flexbox implementation uses `-ms-flex-align` (already included on line 120), not `-ms-align-items`. The invalid property could cause CSS parsing warnings or be ignored by"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "-ms-align-items never existed in any version of IE/Edge; the..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: FEATURE: automatically downsize large images",
      "finding": "[0] app/assets/javascripts/discourse/lib/utilities.js:182\n  Description: Hardcoded limit breaks type-specific configuration: The change replaces `Discourse.SiteSettings['max_' + type + '_size_kb']` with hardcoded `10 * 1024`. This ignores per-type size limits (images vs attachments may have different limits) and prevents administrators from configuring limits via SiteSet"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Hardcoding maxSizeKB = 10 * 1024 ignores Discourse.SiteSetti..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: FEATURE: automatically downsize large images",
      "finding": "[0] app/controllers/uploads_controller.rb:74\n  Description: Method signature conflict introduced by the PR. The new `downsize` method with 3 parameters (line 55 in diff) redefines the existing `downsize` method with 4 parameters (line 53 in diff). In Ruby, the last defined method wins, so the 4-parameter version becomes unreachable. This breaks the API contr"
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "The downsize method is defined twice. The second definition,..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: feat: add calendar cache status and actions (#22532)",
      "finding": "[0] scripts/test-gcal-webhooks.sh:71\n  Description: The sed -i '' syntax is macOS-specific (BSD sed) and will fail on Linux systems with GNU sed. On Linux, sed -i '' interprets the empty string as a filename argument, causing errors. Use a cross-platform approach that works on both macOS and Linux."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'issue'"
      }
    ],
    "metadata": {
      "expected": "issue",
      "case": "logic: macOS-specific sed syntax with empty string after -i..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: Add guest management functionality to existing bookings",
      "finding": "[0] apps/web/components/dialog/AddGuestsDialog.tsx:51\n  Description: Dead code: The check `multiEmailValue.length === 0` will never be true because the initial state is `[\"\"]` (array with one empty string), which has length 1. This guard clause serves no purpose."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "Starting with an array containing an empty string may cause..."
    }
  },
  {
    "vars": {
      "pr_context": "PR: feat: 2fa backup codes",
      "finding": "[0] apps/web/components/auth/BackupCode.tsx:7\n  Description: The component is incorrectly named `TwoFactor` instead of `BackupCode`. This appears to be a copy-paste error from the TwoFactor component. The function name should match the file name and component purpose to avoid confusion and potential issues with React DevTools."
    },
    "assert": [
      {
        "type": "javascript",
        "value": "JSON.parse(output.replace(/```json\\n?/g,'').replace(/```/g,'').trim()).classifications?.[0]?.level === 'warning'"
      }
    ],
    "metadata": {
      "expected": "warning",
      "case": "The exported function TwoFactor handles backup codes and is..."
    }
  }
]